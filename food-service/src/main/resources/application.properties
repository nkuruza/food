spring.datasource.url = jdbc:postgresql://localhost:5432/foodservice
spring.datasource.username = foodservice
spring.datasource.password = p@55w0rd

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

hibernate.connection.provider_disables_autocommit=true

asanda.max-radius=50000

spring.jackson.serialization.fail-on-empty-beans=false

keycloak.principal-attribute=preferred_username

keycloak.auth-server-url=https://auth.asandasystems.co.za/auth
keycloak.realm=virtual-shop
keycloak.resource=vshop-server
keycloak.public-client=true

keycloak.use-resource-role-mappings=true

server.port=8081
server.session.timeout=30

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/opt/tomcat/webapps/uploads/food-service